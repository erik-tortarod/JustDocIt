export enum ETheme {
	DARK = "dark",
	LIGHT = "light",
	SYSTEM = "system",
}

export enum ELanguage {
	SPANISH = "spanish",
	ENGLISH = "english",
}

export enum EEnvironment {
	DEV = "DEV",
	PROD = "PROD",
}
